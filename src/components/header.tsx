import { AppShell, Container } from "@mantine/core";

export function Header() {
  // The header should contain an updating string reflecting the answers
  return (
    <AppShell.Header>
      <Container>
        
      </Container>
    </AppShell.Header>
  )
}
